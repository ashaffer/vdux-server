{
  "name": "vdux-server",
  "description": "Server-side rendering for vdux",
  "repository": "git://github.com/vdux/vdux-server.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean"
  },
  "dependencies": {
    "virtex": "^0.1.18"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.2",
    "standard": "^5.1.0",
    "tape": "^4.2.0"
  }
}
